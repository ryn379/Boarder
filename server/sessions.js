const sessions = {}; // sequence , boardedMatrix, analytics

export default sessions;

// sequence: id,name,queueNumber,passenger,colTier, row , col
// analytics: boarding events (flightId : startTime, endTime, totalPassengers, boardedPassengers, boardedEvents, noShows)
